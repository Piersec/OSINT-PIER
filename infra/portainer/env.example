# Porta HTTPS do painel do Portainer.
PORTAINER_HTTPS_PORT=9443

# 0.0.0.0 permite acesso pela rede local. Restrinja a porta no firewall do host
# para a LAN/VPN e nunca publique o painel diretamente na internet.
PORTAINER_BIND_ADDRESS=0.0.0.0
